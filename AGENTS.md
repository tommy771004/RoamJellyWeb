切勿維持向下相容性：直接移除過時的程式碼路徑，而不是添加相容層（compatibility layers）、後備機制（fallbacks）或遷移方案（migrations）。

選擇能完全滿足當前需求的最簡實作：避免過度預測未來的抽象化（speculative abstractions）、繁複的設定（configuration）與間接呼叫（indirection）。

以分層方式擴展系統：從能夠端到端（end-to-end）運作的最簡版本開始，並在已可正常運作的產品基礎上逐步添加新功能。切勿為了尚未完成的複雜架構，而犧牲一個能正常運作的產品。

保持元件模組化並明確劃分權責（concerns clearly separated）。

優先採用成熟且維護良好的函式庫：前提是它們能降低整體複雜度或提高可靠性。沒有明確理由時，不要重新輪子（重新實作通用功能）。

善用專案中既有的相依套件（dependencies）：在自行編寫實作或新增套件之前，先確認現有資源。在未查閱說明文件與型別定義前，請勿隨意假設某個函式庫缺乏該功能。

以長遠眼光做出一致的架構決策：切勿接受僅能應付當前、預計日後替換的權宜之計（stopgap）。

去除所有矯飾性散文。當有直白陳述可用時，使用它而不是隱喻、華麗辭藻，或是表現作者姿態而非傳達意義的語言。

去除所有矯飾性表達。能直接說明時就直接說明，不要用隱喻、漂亮話或寫作者姿態替代準確含義。
